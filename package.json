{
  "name": "kbju-tracker",
  "version": "1.0.0",
  "description": "Трекер питания с AI анализом фото",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "dev": "cd backend && nodemon server.js",
    "test-prod": "cd backend && node scripts/test-prod-config.js",
    "check-env": "cd backend && node check-env.js",
    "switch-env": "cd backend && node scripts/switch-env.js",
    "test-oauth": "cd backend && node scripts/test-oauth.js",
    "generate-secret": "cd backend && node scripts/generate-secret.js",
    "build": "echo 'No build step required'",
    "postinstall": "cd backend && npm install",
    "deploy:staging": "echo 'Deploy to staging environment'",
    "deploy:prod": "echo 'Deploy to production environment'"
  },
  "keywords": ["nutrition", "tracker", "ai", "food"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.0",
    "multer": "^2.0.1",
    "openai": "^5.7.0",
    "sharp": "^0.34.2",
    "axios": "^1.10.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0",
    "express-session": "^1.18.2",
    "mongoose": "^8.16.0",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 